# AlgDatViz — Mission 2 (Pedagogy & Coverage): Phase 0 Plan

> Status: **awaiting human sign-off (§2 decisions + sequencing).** No code written yet.
> Builds on the completed design-system redesign (see `REDESIGN_PHASE0.md`). The visual foundation is law; this mission adds pedagogy, interaction, coverage, a11y.

---

## 1. Re-audit — brief claims vs the actual repo

| Brief's diagnosis | Verdict after reading the code | Detail |
|---|---|---|
| Recognition-only MCQ checks | **Confirmed** | `TopicTemplate/LessonCheck.jsx` supports only `kind:'choice'` (button row) + `kind:'pair'` (stage selection). All topics use `choice`. No retrieval/free-input/ordering/numeric. |
| Synced pseudocode "not visible" | **Half true — important nuance** | `PseudocodeRail` IS wired in all **7 playgrounds** (+ `FrameTrace`). But the **teaching scrolly stages have no pseudocode at all** — and the scrolly is what most users read. So: add pseudocode+state to the teaching view; verify the playground sync actually locksteps. |
| Scroll-driven, fights the user | **Confirmed** | `TopicScrolly.jsx` advances scenes purely via `IntersectionObserver`; no prev/next/scrub in the concept section. Reduced-motion only stops fades, doesn't add controls. |
| Dark-only, a11y gaps | **Confirmed** | `theme.css` has no `[data-theme]`/`prefers-color-scheme`. Tokens ARE centralized + semantic (`--surface-*`, `--color-text-*`) so light is a token-layer swap — **but** the leftover theme-agnostic `rgba(255,255,255,…)`/`rgba(0,0,0,…)` glass+shadow literals will break in light mode and must be tokenized. Dimmed upcoming-scene text needs a contrast audit. |
| `0/7` with 8 topics | **Confirmed + root cause** | `useProgress.overall.total = PROGRESS_TOPICS.length = 7` (foundations is a `preview` alias excluded on purpose, locked last session), while `HomePage` says "Eight topics." Visible mismatch. Tied to the scope decision below. |
| Half a course; hashing over-built | **Confirmed** | 8 curriculum entries; covers up to graphs/strategies. Hashing (a prerequisite) has a full lesson; the examinable back half (heaps, linear-time sort, MST, SSSP, APSP, max-flow, NP-C) is absent. Hero claims "the TDT4120 curriculum." |
| "stray artifact under hero CTA" | **Identified** | `HomePage` `heroDivider` (a vertical-line+dot SVG after the actions). |

### Reuse inventory (lowers Phase 4–5 cost a lot)
- **Already real step-generators** (pure, reusable, mostly UI-independent): graph `bfs/dfs/dijkstra/kruskal/prim/topo/maxflow` (`utils/graphAlgorithms.js`); sorting `merge/quick/heap/counting/radix/bucket/insertion/selection/bubble` (`utils/sorting/*`, with views); `masterMath.analyseRecurrence/buildLevels`; `coinChangeFrames`, climbing-stairs, interval-scheduling.
- **Genuinely missing implementations:** Bellman-Ford, DAG-SP, Floyd-Warshall, transitive closure, SCC, edge-classification, union-find (as a viz), decision-tree lower bound, randomized-select, LCS, 0/1 knapsack, rod-cutting, Huffman, NP-C/reduction demos.
- **Testability:** only `utils/sorting/*.test.js` exist. `graphAlgorithms.js` is already pure/testable; tree/hashmap/strategies trace logic is partly inside components → extract to pure generators so they can be unit-tested (brief §2).

---

## 2. Decisions — **LOCKED (human-confirmed 2026-06-06)**

1. **Theme default — LOCKED:** follow system (`prefers-color-scheme`) on first load + a manual toggle that persists to localStorage.
2. **Curriculum scope & `0/7` — LOCKED:** show the **full** TDT4120 curriculum on the home map with unbuilt topics as clearly-labelled **"coming soon" locked nodes**; rewrite the hero to be honest ("building toward the full curriculum"); **overall progress counts only built (`ready`) topics**, so the number is always true and grows as Phase 5 ships. (Resolves the count mismatch by construction.)
3. **Teaching interaction model — LOCKED:** keep the narrative but make **explicit playback primary** in the teaching stage (prev/next/play/scrub + keyboard); scroll stays as an optional convenience, never the only path.
4. **Sequencing — LOCKED:** follow brief order — Phases 1–4 (revision layer, interaction, a11y, depth) on existing topics first, then Phase 5 expansion on the finalized template.

---

## 3. Issue → phase map (with repo-specific notes)

- **Phase 1 — Revision layer.** Pseudocode+live-state panel into the teaching stage (driven by `FrameTrace` `{line,state,highlight}`); new `Check` taxonomy in `LessonCheck` (predict-next, numeric/free input with tolerant match, ordering/trace, classify, spot-the-bug, cumulative mixed review); progress tracks correct retrieval; per-topic cheat-sheet + scene-navigator/minimap in `TopicTemplate`; fix the count. *Extend `LessonCheck` + `TopicTemplate`, don't fork.*
- **Phase 2 — Interaction model.** Generalize the playground's `StepControlBar`/`PlaybackEngine` into the teaching stage; delta-highlighting; reduced-motion snaps; scroll demoted. Keyboard + 380px verified.
- **Phase 3 — Light theme + a11y + mobile.** `[data-theme="light"]` token block in `theme.css`; tokenize the residual `rgba(255/0…)` literals; AA audit (start with dimmed scene text); mobile pass; remove `heroDivider`.
- **Phase 4 — Depth on thin sections.** Master theorem → real 3-case `n^log_b a` vs `f(n)` comparison (reuse `masterMath`). Strategies → split greedy vs DP; build overlapping-subproblems + greedy-choice-vs-optimal-substructure.
- **Phase 5 — Back half ⛔ (re-scope with you).** New topics on `TopicTemplate`+`PlaybackEngine`. Proposed sub-sequence (course order, reuse-weighted): **(5a)** Complexity + D&C deepen (mostly exist) → **(5b)** Linear-time sorting + lower bound (sorts exist; build decision-tree + stability) → **(5c)** Heaps/priority queues (heapsort exists; build heap-as-array) → **(5d)** DP + Greedy depth (overlaps Phase 4) → **(5e)** Graph traversal expansion + edge classification/topo/SCC (generators exist) → **(5f)** MST (Kruskal/Prim exist; add union-find + cut) → **(5g)** SSSP: Relax/Bellman-Ford/DAG-SP/Dijkstra (Dijkstra exists; Relax primitive new) → **(5h)** APSP Floyd-Warshall (new) → **(5i)** Max flow (generator exists; add residual/min-cut teaching) → **(5j)** NP-completeness + reductions (all new). Likely 6–10 PRs.
- **Phase 6 — Signature interactives.** One-frontier traversal (FIFO/LIFO/min-dist/min-edge → BFS/DFS/Dijkstra/Prim from one loop); `Relax` shared primitive; master-theorem explorer; notation×case matrix; reduction-direction demo.
- **Phase 7 — Polish & QA.** Consistency, regenerate QA screenshots (top/scroll × light/dark × mobile), update `/styleguide` + the log.

## 4. Guardrails I'm holding to (from §2/§9)
0 hardcoded hex stays 0; lint/build/tests green every phase; new pure trace generators get unit tests; no viz regressions; honest copy; everything on `TopicTemplate`+`PlaybackEngine`+tokens; no bespoke pages; surface big decisions at gates.

## 5. Risks / watch-items
- Light theme's real cost is the glass/shadow `rgba` literals, not the named tokens.
- Bringing playback into the scrolly without losing the narrative feel needs care (Phase 2 is the riskiest interaction change).
- Phase 5 is large; gate each sub-phase. NP-completeness/reductions is the least reuse and the most conceptual.

## 6. Progress log
- **2026-06-06 — Phase 0:** audit + plan; §2 decisions LOCKED by human. Branch `mission2-pedagogy` cut; redesign baseline committed.
- **2026-06-06 — Phase 1a (shared infra): DONE, committed `d33a42b`.** Retrieval check taxonomy (numeric/text/order/classify/predict/spotbug) with pure `checkAnswer.js`; `PseudoState` synced-pseudocode+live-state panel with documented frame contract; cheat-sheet + scene-navigator slots in `TopicTemplate`; `useProgress.recordCheck` (completion now derives from correct retrieval); honest curriculum (7 locked "coming soon" nodes, honest hero, count fixed to /7); `heroDivider` removed. Tests 25→40.
- **2026-06-06 — Phase 1b (7 topics in parallel): DONE, gate passed.** Every topic now has ≥1 non-MCQ retrieval check (graded from real algorithm output, never fabricated), a cheat-sheet, and synced pseudocode + live variable state in its playground driven by a **pure, unit-tested `{line,state}` generator**. Verified on combined tree: lint exit 0, build green, **tests 40→94**, 0 hex. Bugs fixed in passing: tree traversal highlighted the wrong pseudocode line; master-theorem accent token fallback. **Phase 1 DoD met.**
- **Deferred from Phase 1:** cross-topic *cumulative mixed-review* (needs a shared question bank) — schedule before/within Phase 7.
- **2026-06-06 — Phase 2a (teaching-stage playback): DONE by orchestrator** (after the first 2a agent hit a session limit mid-run, having written only `SceneControlBar.jsx`; orchestrator finished the CSS module + wired it into `TopicScrolly`). Explicit scene playback now primary in the concept section: first/prev/play/next/last + scene scrubber, full keyboard (←/→ step, Space auto-advance, Home/End), scoped so it never hijacks the wheel or typing; scroll stays optional & cooperative (IntersectionObserver still syncs); `prefers-reduced-motion` snaps instantly; auto-advance is **gated on answering the active scene's check** (retrieval before progress); per-topic hue tint; usable at 380px. All topics inherit it free. Verified: lint 0, build green, 94/94 tests, 0 hex; visual QA confirms (violet Trees, gold Master, "answer the check to continue"). **Phase 2 DoD (keyboard-operable + reduced-motion + no scroll-jack) met** for teaching stage; playgrounds already keyboard/reduced-motion via PlaybackEngine.
- **Deferred to a decision:** per-topic *delta-highlighting* ("animate what changed each step") from brief §4 — DECIDED: deferred to Phase 7 motion refinement.
- **2026-06-06 — Phase 3a (light theme + contrast): DONE, gate passed.** Light theme at the token layer (`[data-theme='light']` overrides semantic surface/text/border/shadow tokens + `--topic-l/-s` for legibility on white; the 8 hues + washes + states derive automatically). Theme follows OS on first load + persisted manual toggle (`useTheme` + `ThemeToggle` in sidebar footer) with an anti-flash inline script in `index.html`. ~95 theme-agnostic white/black literals tokenized via `--glass-tint`/`--scrim-ink` (dark byte-equivalent). AA fixes: dimmed inactive scene text 0.36→0.62 (was ~1.9:1); sidebar active label → neutral text in light. Verified: lint 0, build green, 94/94, **0 hex AND 0 raw white/black**; visual QA in both themes confirms. **Phase 7 carry-overs (logged by agent):** gold(48°)/teal(162°) topic hues can't hit 4.5:1 as *small text* on white — remaining offenders are ~10px AlgorithmView phase-labels + topic-fill button text; ~80 brand `rgba()` literals in AlgorithmViews don't follow the lowered light topic lightness. Both = Phase 7 per-spot a11y pass.
- **2026-06-06 — Phase 3b (mobile): DONE, gate passed.** Verified the whole app at 380px via `scripts/qa-capture.mjs` (now `PW_THEME`/`PW_W`/`PW_H`/`PW_LABEL`/`PW_SCROLL`-driven): sidebar collapses to a 64px icon-rail (≤720), scrolly stacks stage-over-prose (≤960), `SceneControlBar` + checks + both playgrounds (hash sandbox, sorting sandbox) all usable at 380px. Only fix needed: hide the cramped "SOON" badge in the rail (lock icon already signals locked). lint 0, build green.
- **✅ PHASE 3 COMPLETE.** DoD: theme toggle site-wide (system default + persisted, no flash) ✓; 0 hardcoded color (0 hex AND 0 raw white/black) ✓; mobile verified at 380px ✓; reduced-motion honored ✓; hero `heroDivider` artifact removed (Phase 1a) ✓. **AA: met except two documented small-text exceptions** (gold 48° / teal 162° topic hues as <13px text on white, in sorting AlgorithmView phase-labels + topic-fill button text) — slated for the Phase 7 per-spot a11y pass.
- **2026-06-06 — Phase 4 (depth on thin sections): DONE, gate passed.**
  - **Master Theorem:** found to already meet its Phase-4 goal (C5) — Phase 1b had added the per-level work bar chart (leaves-vs-combine shape → Case 1/2/3, dominant side highlighted, live Θ verdict) + scenes walking `c=log_b a` vs `d` across all three cases + compute-c + classify-cases. No rework needed; left as-is.
  - **Strategies:** deepened to F6/F7/G2. New `overlapping-subproblems` scene + `RecursionBoard` (Climbing Stairs naive tree with repeated subproblems highlighted → collapses to a linear memo table — the DP signature made visible), backed by a pure unit-tested census generator `climbingStairsRecursion.js`. New `two-properties` scene + `DecisionBoard` separates the three ideas: optimal substructure (shared) + greedy-choice → greedy; optimal substructure + overlapping subproblems → DP. Revised `dp-remembers`/`greedy-safe`/`choose-what` + cheat-sheet to name all three. New retrieval checks (numeric overlap count, classify).
  - Verified: lint 0, build green, **tests 94→102**, 0 hex AND 0 raw white/black; visual QA confirms the new scenes (incl. rose-hue + numeric check).
- **2026-06-07 — Phase 5 ⛔ scoped + signed off:** human chose **course order, 2 topics per batch**, each batch gated. Orchestrator owns shared wiring (hues, curriculum, routes); topic agents own only their dir + page.
- **2026-06-07 — Phase 5a (batch 1: Linear-time sorting + Heaps): DONE, gate passed.** Two new topics built on `TopicTemplate` + `PlaybackEngine`, inserted in course order (sorting→**linear-time-sorting**→hashing→trees→**heaps**→graphs…; renumbered). Shared wiring by orchestrator: 2 harmonized hues (`--topic-linsort` green 128°, `--topic-heaps` magenta 312°), 2 curriculum entries flipped soon→ready, 2 Sidebar icons, 2 App routes. **Linear-time sorting** (D1–D7): decision-tree Ω(n log n) lower bound + counting/radix/bucket (reusing existing pure sort generators/views) + stability (incl. why radix needs a stable subroutine), 3 new tested generator modules. **Heaps** (E1–E6): heap-as-array tree↔array dual view + sift/insert/extract + Build-Max-Heap-O(n)-vs-repeated-insert (E1) + PQ framing, tested heap generators. Progress denominator 7→9. Verified: lint 0, build green, **tests 102→140**, 0 hex AND 0 raw white/black; visual QA confirms both (green/magenta hues, scene-navs, dual-view stage, numeric checks).
- **2026-06-07 — Phase 5a (batch 2: MST + Single-source shortest paths): DONE, gate passed.** Two graph-family topics on `TopicTemplate` + `PlaybackEngine`, after graphs/strategies (numbers 11/12). Shared wiring by orchestrator: 2 hues (`--topic-mst` yellow-green 88°, `--topic-sssp` blue-violet 268°), 2 curriculum entries flipped soon→ready, 2 sidebar icons (Share2/Route), 2 App routes. **MST** (I1–I6): own unit-tested union-find (path compression + union by rank) + cut/light-edge helpers; cut-property stage; Kruskal & Prim on the same graph → same tree (weight 27), with synced pseudocode showing union-find components / frontier. **Shortest paths** (J1–J12): **`Relax` built as the one shared primitive** — Bellman-Ford / DAG-SP / Dijkstra are the same `relax()` in different orders; live dist[]/pred[] table, negative-edge + negative-cycle scenarios, unit-tested generators incl. neg-cycle detection. Progress denominator 9→11. Verified: lint 0, build green, **tests 140→178** (+38), 0 hex AND 0 raw white/black; visual QA confirms both. (Bonus: this delivers the Phase-6 "Relax shared primitive" signature interactive early.)
- **2026-06-07 — Phase 5a (batch 3: All-pairs SP + Maximum flow + NP-completeness): DONE, gate passed.** Human asked for all 3 remaining at once. Shared wiring by orchestrator: 3 hues (`--topic-apsp` lime 108°, `--topic-maxflow` gold-green 68°, `--topic-npc` cyan 178°), 3 curriculum entries flipped soon→ready (the **last** ones — removed the now-unused `comingSoon` factory + stale comments), 3 sidebar icons (Grid3x3/Workflow/Puzzle), 3 App routes, and **honest full-coverage home copy** (no more "coming soon"). **APSP** (K1–K4): Floyd-Warshall matrix-fill-across-k + predecessor reconstruction + transitive closure; tested generators. **Max flow** (L1–L14): residual network + augmenting paths + Ford-Fulkerson/Edmonds-Karp + **max-flow=min-cut reveal** + integrality + bipartite matching; tested (CLRS network = 23, min-cut = max-flow). **NP-completeness** (M/N): P/NP/NPC/NP-hard, verify-vs-solve made tangible (certificate verifiers), the standard NPC problems, and **reduction DIRECTION** (incl. a spotbug for the inverted-arrow fallacy); tested verifiers + a 3-SAT≤p-IndependentSet reduction. Progress denominator 11→14. Verified: lint 0, build green, **tests 178→234** (+56), 0 hex AND 0 raw white/black; visual QA confirms all 3 + the now-complete home map.
- **✅ PHASE 5 COMPLETE — the full TDT4120 curriculum is built (15 topics; the brief's central "half a course" problem is resolved; hero copy now honestly claims full coverage).**
- **Remaining mission work:** **Phase 6** — signature interactives: the **one-frontier traversal** (FIFO/LIFO/min-dist/min-edge → BFS/DFS/Dijkstra/Prim from one loop) is the last one (the `Relax` shared primitive already shipped with SSSP; master-theorem explorer, notation×case matrix, and reduction-direction demo also effectively shipped within their topics). **Phase 7** — polish/QA: deferred small-text AA fixes (gold/teal), delta-highlighting, cross-topic cumulative review, full QA against §5/§8.
- **2026-06-07 — Phase 7a (a11y + styleguide + mobile for the back half): DONE, gate passed.**
  - **WCAG AA small-text fixes (token-driven).** Root cause: gold (master 48°) + the yellow-green band (graphs 162°, linsort 128°, mst 88°, apsp 108°, maxflow 68°, npc 178°) can't reach 4.5:1 as <13px text on white at the light family lightness (l38: 3.0–3.6:1). Added two token families in `theme.css`: a `--topic-l-ink` knob (= `--topic-l` in dark, **30%** in light) driving per-topic `--topic-*-ink` (+ `--color-accent-{green,orange,yellow}-ink`) for small COLORED text — clears AA in both themes (light 4.6–12:1; dark unchanged at 12:1); and per-topic `--topic-*-contrast` (the readable text color to place ON a solid topic fill — white on deep hues, deep ink on the light band; dark stays dark-ink everywhere). `TopicTemplate` publishes the active topic's `--topic-accent-ink`/`--topic-accent-contrast` on the page root (derives the suffix off the resolved accent string, so accent-passing pages where the token suffix ≠ topicId — linsort/sssp/maxflow/npc/master/stacks — resolve correctly). Repointed offenders: AlgorithmView `.phaseLabel`/badges (green→`-green-ink`), the shared `LessonCheck.submit` (every topic's check button: now `--topic-accent-contrast`, 4.5–10.9:1 light / 4.8–14.5:1 dark — was white-on-lime 3.4:1), `LessonCheck` hover/order/pair-icon, `SceneControlBar` play/meta, `TopicScrolly` scene index, ThemeToggle sun icon, HomePage completed-node check, and all `color: var(--topic-accent)` / `--color-text-on-accent`-on-topic-fill spots across the 7 back-half topics + MasterTheorem. **Verified every topic's submit + ink ≥ AA in BOTH themes.**
  - **Styleguide = source of truth.** `/styleguide` now shows **all 15** topic hues (added linsort, heaps, mst, sssp, apsp, maxflow, npc); subtitle states the dark/light family values + the `-ink` rule. Renders in both themes (15 swatches each).
  - **Mobile (380px) for the 7 new topics.** Real overflow found + fixed: the shared `StepControlBar` dock bar (~433px: 5 buttons + 220px scrubber + meta + speed) overran phones → added a ≤440px wrap (scrubber drops to its own full-width row, dividers hidden); `TopicScrolly` ≤960px column was a bare `1fr` whose min-content floor let a wide stage figure push past the viewport → `minmax(0,1fr)` + `min-width:0` on the stage/prose tracks; `LessonCheck` classify `<select>` overran its row → wrap + `min-width:0`/`max-width:100%`; long hero titles (e.g. "All-Pairs Shortest Paths") mid-word-broke → `overflow-wrap` + a ≤540px hero step-down to display-xl. **Result: 0 document horizontal overflow on all 7 topics in both themes; matrices/dist-pred table/residual network/heap dual-view/decision-tree/NPC explorer all usable at 380px** (verified via Playwright at 380px).
  - Guardrails held: lint 0, build green, **tests 247/247** (234 + sibling's 13 oneFrontier), **0 hex AND 0 raw white/black** in module CSS. Did not touch the Graph topic / `graphAlgorithms.js` / `App.jsx` / `curriculum.js` / `useProgress.js`.
  - **Deferred (noted, not blocking):** the ~80 brand `rgba()` literals in the sorting AlgorithmViews still use fixed dark-theme alphas (state tints) — they don't track the lowered light topic lightness but are colored (not hex/white/black, so invariants stay 0) and meet 3:1 as fills; a future pass could tokenize them. Delta-highlighting + cross-topic cumulative review remain separate Phase-7 tasks.
